Whisper 语音识别模型包

这个安装包只包含一个 Whisper 模型，用于把视频声音识别为字幕；它不是完整软件。请先安装 YouTube Chinese Localizer 基础程序，再把模型安装到同一个程序文件夹。

选择建议：

- Whisper Small：推荐大多数电脑使用，速度更快、内存和显存要求较低。
- Whisper Medium：适合内存/显存更充足的电脑，识别质量更高，但速度较慢、体积更大。

模型安装后，程序会在开始任务前自动检测并选择可用模型。以后可以安装另一个模型包；两个模型可以共存。
