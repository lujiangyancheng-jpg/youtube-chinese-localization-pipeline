# ChatGPT Plus and OpenAI API billing are separate.
# These values are optional in Phase 1 because manual export/import requires no API key.
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_ENDPOINT=https://api.openai.com/v1
OPENAI_COMPATIBLE_MODEL=
