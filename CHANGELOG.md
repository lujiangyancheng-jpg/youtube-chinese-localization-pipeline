# 更新日志

本文件记录已发布版本中用户可见的新增、优化与修复。每次发布新版本前，必须先在这里补充条目，再同步代码、安装脚本和说明到 GitHub。

## 0.7.0.10 — 2026-08-24（正式版）

- 项目品牌：GitHub 仓库更名为 `video-localizer`，首页统一使用 Video Localizer 项目名，同时保留桌面程序名 Localize Studio。
- 社区入口：重构 GitHub 首页，增加快速下载、30 秒产品说明、适用人群、测试招募、参与方式与当前成熟度；启用 Discussions 作为使用交流区。
- 测试协作：新增 15 分钟测试清单、贡献指南、结构化 Bug／功能建议表单以及 Pull Request 模板，方便非开发者提供可复现反馈。
- 链接迁移：程序内更新检查、帮助中心、模型下载和安装包发布者地址全部迁移到新仓库；网络请求允许跟随 GitHub 的官方仓库重命名重定向。
- 模型兼容：继续复用已验证的 `0.7.0` Whisper 与 Local AI 模型包，升级程序无需重新下载模型。

## 0.7.0.9 — 2026-08-23（正式版）

- 稳定通道发布：本版作为 0.7.0 系列的首个正式版发布，GitHub Release 不再标记为 Pre-release；新安装默认使用“稳定”更新通道。
- 续跑任务绑定：浏览器抓取前的自动续跑请求会绑定到当时的确切任务列表；用户在等待期间修改输入时，旧请求会立即失效，不会误启动新链接。
- 抓取结果校验：只有抓取的播放页确实属于原待启动队列时，才会把续跑请求安全转移到新媒体 URL。
- 发布政策收口：正式发布清单明确区分程序四段迭代与三段模型 ABI，允许在模型 ABI 未变时复用已验证模型包，并强制披露代码签名状态与 SHA-256。
- 验收范围：覆盖完整自动化测试、Windows CI、隔离安装、原位升级、模型保留、原生启动器、公开 HTML5 页面与目标动态页抓取、GitHub 资产回下载哈希比对。

## 0.7.0.8 — 2026-08-23（开发迭代）

- 启动前强制分流：预分析发现 Cloudflare、iframe 或动态脚本播放页时，“开始本地化”不再把原始页面 URL 交给下载管线，而是先打开独立 Edge 完成媒体抓取。
- 一次点击续跑：完整媒体 URL 抓取成功会自动触发新地址预分析，验证通过后自动继续原任务，不需要再点第二次开始。
- 竞态修复：即使在媒体预分析完成前快速点击开始，任务也会先等待分析结果，不会绕过浏览器抓取。
- 取消安全：关闭或取消抓取会清除自动续跑状态，任务不会在之后意外启动，下次仍可重新进入抓取。
- 回归验证：新增动态页错误识别、队列中目标页选择与开始前管线阻断测试。

## 0.7.0.7 — 2026-08-23（开发迭代）

- 浏览器辅助抓取：动态播放页无法通过普通 HTML 解析时，可启动使用全新临时配置的 Microsoft Edge；视频开始播放后，程序通过本机 DevTools 通道捕获播放器的完整 HTTP(S) 媒体地址并自动替换原播放页。
- 自动恢复：预分析发现 Cloudflare 浏览器验证时会自动切换到浏览器抓取；升级前保存的同类失败任务也会在恢复后直接进入新流程，无需重新建立队列。
- 目标站点验证：已在 `https://www.lmm85.com/play/8164_1_1.html` 实测捕获播放器当前的 `groupvideo.photo.qq.com` 完整 MP4 地址，并在公开 HTML5 测试页验证通用兼容性。
- 隐私隔离：不连接用户日常 Edge 配置，不读取或持久化 Cookie、登录状态、密码、Authorization、请求头或响应正文；临时浏览器关闭后清理独立配置目录。
- 访问边界：需要浏览器验证时由用户在可见窗口中亲自完成；程序不自动解决挑战、不绕过 DRM、付费、登录、地区或内容权限，且继续拒绝 localhost、局域网与保留地址。
- 体积控制：复用 Windows 自带 Edge 和现有 `websockets` 运行库，不捆绑 Chromium/Playwright 浏览器内核，Standard 安装包不会因此增加数百 MiB。
- 验证：新增 CDP 媒体事件过滤、播放器 `currentSrc` 优先级、敏感请求头不进入结果、Edge 定位、浏览器抓取输入与错误恢复测试。

## 0.7.0.6 — 2026-08-23（开发迭代）

- 专用直链入口：主界面新增“媒体直链”，可一次加入 MP4、WebM、M3U8、MPD 与 `groupvideo.photo.qq.com` 这类无扩展名 CDN 地址，然后继续使用预分析、最高质量下载、字幕和断点续跑流程。
- 输入检查：加入任务前拒绝播放页、登录凭据、已知过期的时间签名和以 `…` 结尾的截断地址，避免长时间运行后才失败。
- CDN 兼容：无扩展名地址先做 HEAD 检查；对拒绝 HEAD 但允许媒体 GET 的服务器，自动改用不读取响应正文的字节范围探测，减少误判。
- 故障说明：针对 HTTP 401/403、404/410 和 429 分别显示“依赖浏览器凭据或过期”、“地址已失效”与“暂时限流”的可执行恢复建议。
- 安全边界：仅处理用户提供的完整公开媒体地址；不读取浏览器 Cookie、不接管登录会话，也不绕过 DRM 或站点访问控制。
- 验证：新增无扩展名 CDN、截断地址、过期 epoch 签名、HEAD 403 后 Range GET 回退和专用界面输入检查测试。
- 发布稳定性：安装包的桌面冒烟测试使用隔离的应用数据目录，不再读取或改动开发电脑上保存的真实任务队列。

## 0.7.0.5 — 2026-08-23（开发迭代）

- 新增公开播放页解析：同一“粘贴链接”入口现在可读取用户有权下载、并在标准 HTML5 `<video>`/`<source>`、Open Graph、Twitter Card 或 VideoObject JSON-LD 中明确声明媒体地址的网页；解析后的 MP4/HLS/DASH 继续使用最高质量下载、预分析、字幕与断点续跑流程。
- 安全边界：网页读取限制为 2 MiB HTML 与最多 5 次重定向；每个页面和候选媒体主机都必须解析为公网地址，阻止 localhost、局域网、链路本地和保留地址，且拒绝 URL 内嵌用户名或密码。
- 访问控制：不执行或反混淆站点脚本、不递归抓取第三方 iframe、不读取 Cookie、不模拟登录、不解决 Cloudflare 等反爬挑战，也不绕过 DRM、付费或地区限制；失败时会直接说明属于哪一种边界。
- 稳定恢复：网页本身作为项目的稳定来源与发布署名地址，临时媒体直链只用于实际下载；未完成的网页任务会重新解析页面以刷新可能过期的媒体签名，已完成阶段仍按内容指纹跳过。
- 隐私与体验：网页解析结果不进入桌面短期缓存，避免把临时媒体签名持久化；任务中心与帮助页同步增加“公开 HTML5 播放页”说明。
- 验证：新增声明式 HTML/JSON-LD、相对地址、iframe-only 拒绝、私网/凭据拦截、Cloudflare 错误、网页来源元数据与续跑刷新测试。

## 0.7.0.4 — 2026-08-21（开发迭代）

- 队列恢复：逐视频任务中心采用版本化本地状态。应用关闭、意外退出或电脑重启后会恢复输入、标题、媒体摘要、进度和项目路径；原本运行中的项目会安全显示为“已暂停，可继续”，不会伪装成仍在后台运行。
- 单任务控制：运行中可只暂停所选项目而不影响其他视频；暂停或失败的项目可单独继续，也可整批恢复。任务完成后可从任务中心直接打开最终 MP4，项目阶段仍按已有指纹断点续跑。
- 检查缓存：桌面预分析与正式管线共享短期媒体检查结果，避免开始任务时立刻重复访问同一来源。YouTube 缓存 30 分钟，时效媒体直链缓存 5 分钟；本地文件变化会立即失效，带签名直链不会原样写入缓存。
- 自适应并发：不再固定两个并行项目。根据逻辑 CPU 和系统内存自动选择 1–4 个下载／预处理任务；Whisper、本地 AI 与编码仍受跨进程性能槽保护，兼顾高配电脑吞吐与低配电脑响应。
- 进度与提醒：下载状态新增实时速度和剩余时间；整批成功、部分失败、暂停或停止后会响铃并闪烁 Windows 任务栏，长任务无需一直盯住窗口。
- 界面适配：顶部主操作与维护工具改为两行响应式排列，默认窗口和 900 像素最小宽度下都不会再因按钮过多而横向溢出。
- 隐私与可靠性：队列状态和媒体缓存均采用原子写入；API Key 不会持久化，媒体缓存不保留输入直链的查询签名，损坏缓存会直接回退重新检查。
- 验证：新增队列重启迁移、暂停不计失败、缓存 TTL／本地文件失效／管线复用、自适应并发、下载 ETA 和完成提醒测试。

## 0.7.0.3 — 2026-08-17（开发迭代）

- 界面：新增逐视频任务中心。批量粘贴后可直接看到每个视频的标题、媒体信息、独立处理阶段和进度，不再只显示整批任务的一条总进度。
- 预分析：粘贴 YouTube、授权媒体直链或选择本地视频后，会在后台读取标题、时长、分辨率、帧率和预估下载大小；分析不会下载视频，也不会阻塞界面或开始正式项目。
- 队列：任务中心可单独移除所选视频；正式处理创建项目后，可从对应任务直接打开项目文件夹。运行期间会锁定输入，避免误改队列导致状态错位。
- 设置：翻译方向、字幕模式、翻译方式、字幕字号和位置、输出画质／帧率／分辨率、输出目录、更新通道和断点续跑会在本机保存并在下次启动恢复。
- 隐私：桌面设置文件采用独立、版本化结构；API Key 从不写入该文件。损坏或旧版设置会安全回退，不会阻止应用启动和处理。
- 验证：增加设置迁移与异常回退、媒体大小估算、预分析分发、显示标签恢复和任务中心 GUI 构造测试。

## 0.7.0.2 — 2026-08-14（开发迭代）

- 界面：将原“首次设置”改为常驻“帮助中心”，集中提供快速教程、环境与模型状态、兼容模型下载和常见问题；取消启动后自动弹出教程。
- 修复：完成引导不再绑定程序小迭代版本，已有用户升级 `0.7.0.x` 后不会再次被当成首次使用。
- 修复：四段式程序版本的 Whisper 下载入口改为前三段模型兼容 Release；例如 `0.7.0.2` 会准确打开 `v0.7.0` 模型页面。
- 状态：空白首页直接显示已安装 Whisper、本地 AI 和自动资源调度模式；帮助中心同时说明输出磁盘是否适合高画质任务。
- 易用性：帮助中心可一键选择“只下载最高画质”或“翻译并压制字幕”，并说明多链接队列与断点续跑方式。
- 故障恢复：针对 YouTube 429、CUDA/显存不足、磁盘空间不足、受限视频和 FFmpeg 字幕压制失败显示简短、可执行的恢复建议。

## 0.7.0.1 — 2026-08-14（开发迭代）

- 版本：采用四段版本号。前三段表示产品与模型兼容阶段，第四段表示该阶段内的程序小迭代；`0.7.0.x` 可继续使用 `0.7.0` Whisper 与 Local AI 模型包，无需反复下载数 GB 模型。
- 恢复：桌面端队列发生失败或手动停止后，可只重试未完成项目；重试会强制使用断点续跑，已完成下载、识别、翻译和压制阶段不会重复执行。
- 完整性：新完成的阶段会记录输出文件大小和内容指纹。续跑会发现空文件、被截断视频或被意外修改的字幕，并只重做受影响阶段；旧项目会在首次续跑时自动升级状态格式。
- 界面：输出目录会提示可恢复的未完成项目数量；重新粘贴原链接并保留“继续上次处理”即可恢复。
- 性能：重任务改为硬件分级调度。低余量电脑继续严格串行；具备至少 24 GiB 内存和约 10 GiB NVIDIA 显存的电脑可让 AI 计算与视频编码安全重叠，同时仍禁止两个 AI 任务争抢显存。
- 更新：新增“稳定／开发”更新通道。开发通道可发现 GitHub 预发布的小迭代，稳定通道只提示正式版本。
- 安装：四段程序小迭代复用前三段对应的模型包及其 SHA-256；Standard 构建可直接读取已发布模型资产摘要，不需要在本机重新生成 11.8 GiB 模型包。
- 诊断：管线会明确记录项目工作目录。桌面端可识别的失败项目会在后台生成脱敏诊断包，便于开发期定位故障；包内不含视频、字幕正文、链接、路径或凭证。
- 验证：增加队列恢复选择、输出篡改检测、旧状态升级、硬件调度、更新通道、项目目录边界和失败任务筛选测试。

## 0.7.0 — 2026-08-14（开发版）

- 优化：Standard 改为单文件安装程序，不再需要手动下载和保管 `Setup-1.bin`。实测从约 463 MiB 降至 288.64 MiB，减少约 37%。标准版只携带一套经过功能校验的精简 FFmpeg，保留 libass 字幕、NVENC、QSV、AMF 和 CPU 编码，不再重复携带整套兼容副本。
- 优化：移除仅在构建期间使用的 pip、setuptools、hatchling 和多余命令行包装器；保留 Deno，避免影响 YouTube 的 JavaScript 兼容性。
- 界面：新增与“视频下载 + 双语字幕本地化”主题一致的应用图标，用于应用窗口、原生启动器、桌面快捷方式、安装器和卸载项。
- 修复：更正 Authenticode 签名失败信息中的 PowerShell 变量解析，避免在接入真实代码签名证书后因脚本语法中断发布。

## 0.6.9 — 2026-08-14（开发版）

- 新增：Standard 基础安装器新增“选择本地模型”页面，可在安装时勾选 Whisper Small、Whisper Medium 和本地 AI 段落翻译（Qwen3:4b + Ollama）。未勾选的模型不会下载或占用安装空间。
- 安全：安装器从固定的同版本 GitHub Release 下载可选模型包，并在下载完成后逐个校验 SHA-256；模型包仍会拒绝安装到缺失或版本不匹配的基础程序。
- 优化：Qwen/Ollama 拆分为独立的 Local AI 模型包；完整离线包和本地 AI 模型包只打入 Qwen3:4b 实际引用的文件，避免把构建电脑中无关的 Ollama 模型一并带入发布包。

## 0.6.8 — 2026-08-13（开发版）

- 新增：安装包内置 `Localize Studio.exe` 原生 Windows 启动器。开始菜单和桌面快捷方式不再通过 `.cmd` 启动；启动器使用 Unicode 安全路径并在启动前检查核心离线组件。
- 安全：Whisper Small/Medium 模型包必须安装到同一版本的基础包；错误目录、缺失基础包或版本不匹配会在写入文件前停止。
- 优化：桌面端多视频队列最多同时运行两个项目；下载和预处理可重叠，Whisper、本地 AI 与压制继续通过共享资源锁排队，避免抢占显存和编码器。
- 优化：对公共来源的短暂 429/5xx 下载限制进行有限退避重试；持续失败会明确提示等待后使用“继续处理”，不会读取 Cookie 或绕过访问限制。
- 发布基础设施：新增安装包构建、Unicode 自定义路径、启动器验证和故障恢复的发布验收清单；加入可选 Authenticode 签名入口，需使用真实证书。

## 0.6.7 — 2026-08-12

- 新增：桌面端“字幕审核”窗口，可按项目加载目标字幕、逐条修改文字和换行，同时保留原始时间轴。
- 新增：保存时自动重建目标 SRT 与 ASS；中英双语项目会同步重建双语 SRT/ASS，避免字幕文本已修改但压制样式仍是旧版本。
- 新增：从当前字幕段落开始生成 12 秒真实视频预览，方便在完整重新压制前检查字幕内容、位置、字号和画面效果。
- 安全：预览写入独立 `review_preview_*.mp4`，不会覆盖最终成片；最终视频需由用户确认后再完整压制。

## 0.6.6 — 2026-08-12

- 新增：首次启动三步引导，可选择“只下载”或“制作字幕”，并清晰显示基础包与 Whisper 模型包的就绪状态。
- 新增：缺少 Whisper 时可从应用内直接打开本项目的官方下载页；明确提示 `Setup.exe` 与 `.bin` 分卷必须位于同一文件夹。
- 新增：应用内“检查更新”。仅在用户主动点击后访问公开 GitHub Release，不发送视频链接、路径、硬件信息或 API Key。
- 发布：统一提供 Standard、Complete、Whisper Small 与 Whisper Medium 的同版本离线包；每个分卷均附 SHA-256 校验清单。
- 验证：通过完整自动测试、GUI 冒烟测试、Standard/Complete 隔离安装验收，以及 Complete 的本地 Qwen 推理验证。

## 0.6.5 — 2026-08-11

- 优化：Standard 基础安装包改用高压缩 LZMA2 分发，保留 Python、FFmpeg、显卡兼容回退、字体和两套中英快速翻译模型，降低下载与保存体积。
- 保持：Complete 包仍不压缩大模型文件，避免安装时间明显增加且不产生有效体积收益。

## 0.6.4 — 2026-08-11

- 新增：英文或简体中文可翻译为日语、韩语、西班牙语、法语、德语、葡萄牙语、俄语或阿拉伯语，并生成对应语种的 SRT、ASS、硬字幕 MP4 和可选字幕 MP4。
- 规则：新增语种只允许使用本地 AI 段落翻译（Ollama）或 OpenAI-compatible API；快速离线模型和人工翻译不会被错误用于这些语种。
- 优化：界面会按所选方向自动隐藏不兼容的快速离线、人工翻译和中英双语字幕选项，并说明所需的本地模型或 API。

## 0.6.3 — 2026-08-11

- 新增：支持无扩展名的 CDN 视频直链；发送极小的 HTTP 验证请求，确认服务器声明为 `video/*`、HLS 或 DASH 后才交给下载器。
- 修复：不再仅依赖 `.mp4` 等路径后缀，因此 Akamai 等使用不透明路径的视频直链可以正常处理。
- 安全边界：HTML 播放页仍在网络请求前拒绝；不会抓取网页、读取 Cookie 或绕过访问保护。

## 0.6.2 — 2026-08-11

- 新增：支持有授权的直接媒体地址：`.mp4`、`.webm`、`.mov`、`.mkv`、`.m3u8` 与 `.mpd`。
- 新增：桌面端和命令行均提示“实际媒体地址”与播放页的区别。
- 优化：对含临时签名参数的直链，重新粘贴同一路径的新链接并断点续跑时，会刷新地址而不是新建项目。
- 安全边界：不抓取播放页，不读取 Cookie 或登录信息，不支持 DRM、付费或其他访问保护的媒体。

## 0.6.1 — 2026-08-11

- 新增：16:9 字幕布局预览；可拖动字幕位置和右下角控制点调整字号。
- 新增：字幕水平、垂直位置可写入任务并传给命令行。
- 优化：最终 ASS 字幕与压制视频使用预览确认的位置和字号。
- 验证：完成 Windows 基础包、Whisper 模型包、GUI 与完整测试验证。

## 0.6.0 — 2026-08-10

- 新增：Whisper Small、Whisper Medium 拆分为可选独立模型包，减少基础安装包体积。
- 新增：固定内置 Noto Sans CJK SC 字体，并提供小号到超大字幕字号。
- 优化：安装前检查已安装模型；不再静默下载 Whisper，缺失时给出明确指引。
- 优化：Standard 与 Complete 基础包按本地快速翻译和本地 AI 段落翻译需求拆分。

## 0.5.9 — 2026-08-10

- 新增：任务开始前硬件、磁盘空间、模型包和安装层级预检。
- 优化：根据已安装模型、显存和系统内存自动选择安全的 Whisper 与翻译回退方案。
- 优化：安装包和哈希清单按层级生成，降低下载和安装失败风险。

## 0.5.8 — 2026-08-10

- 优化：离线安装包包含 CUDA 运行时发现与诊断，避免不完整 GPU 环境造成卡死。
- 优化：离线配置和发布诊断更稳健，不能安全使用 GPU 时直接采用 CPU `int8`。

## 0.5.7 — 2026-08-10

- 新增：离线安装包验证流程，检查应用清单、模型、字体与启动能力。
- 优化：发布前验证和失败提示更清晰。

## 0.5.6 — 2026-08-10

- 新增：按可用硬件自动适配 NVIDIA NVENC、Intel Quick Sync、AMD AMF 或 CPU H.264 渲染。
- 优化：输出编码根据实际机器能力选择，而不是假定某种显卡一定可用。

## 0.5.5 — 2026-08-10

- 优化：适配稳定版 NVIDIA 驱动，并增加兼容 FFmpeg 的 NVENC 探测与回退。

## 0.5.4 — 2026-08-10

- 优化：本地重负载步骤加入资源调度，降低多任务同时占满显存、CPU 和磁盘的风险。

## 0.5.3 — 2026-08-10

- 优化：桌面端显示下载、离线翻译和压制的真实进度。
- 优化：GPU 不可用时更快、更稳定地切换到安全回退路径。

## 0.5.2 — 2026-08-09

- 新增：智能高质量输出控制，可保持源分辨率/帧率，或限制到 4K、2K、1080p、720p 和 60/30 FPS。
- 优化：始终优先保留最高源画质、最高源帧率和最佳音频；不补帧、不无谓放大。

## 0.5.1 — 2026-08-09

- 修复：后续渲染报告不会覆盖此前下载、识别阶段已记录的警告。

## 0.5.0 — 2026-08-09

- 新增：自动、快速、均衡、质量和安全 CPU 性能配置。
- 新增：字幕质量审查，标记阅读速度、重复、闪现和过长行等问题。

## 0.4.3 — 2026-08-09

- 修复：改进 Whisper 的 CUDA 运行时发现，降低已装 NVIDIA 环境却无法使用 GPU 的概率。

## 0.4.2 — 2026-08-04

- 修复：Whisper 的 CUDA 推理失败后自动以 CPU `int8` 重试，避免整项任务失败。

## 0.4.1 — 2026-08-04

- 优化：简化桌面工作流，突出粘贴链接、授权确认、开始、停止和输出目录操作。

## 0.4.0 — 2026-08-04

- 优化：更新桌面界面并将 Tk 运行时纳入 Windows 包，减少启动与系统环境问题。

## 0.3.2 — 2026-08-04

- 新增：仅下载原视频（无字幕）模式，按最高可用视频流与最佳音频下载并合并。

## 0.3.1 — 2026-08-04

- 新增：内置字幕字体选择，确保不同 Windows 电脑上中文渲染一致。

## 0.3.0 — 2026-08-04

- 新增：完整离线模型安装包；本地翻译不再依赖 API Key。

## 0.2.0 — 2026-08-03

- 新增：本地 AI 段落翻译；先按上下文理解完整段落，再切分为自然字幕。

## 0.1.2 — 2026-08-03

- 新增：FFmpeg 压制进度显示与中断后恢复能力。

## 0.1.1 — 2026-08-03

- 修复：字幕时间轴对齐与双语字幕数量不一致的处理。
- 新增：无需 API 的离线本地化基础能力与更完整的 yt-dlp 依赖。

## 0.1.0 — 2026-07-27

- 首个可用版本：YouTube/本地视频项目目录、字幕解析、翻译导入导出、FFmpeg 压制、报告、日志和断点续跑。
- 新增：Windows “粘贴链接”启动器，以及英语→中文、中文→英语的本地化工作流。
